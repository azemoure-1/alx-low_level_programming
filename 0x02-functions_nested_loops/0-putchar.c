#include "main.h"

/**
 * main - Entry point
 *
 * Description: print _putchar using prototype
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char str[] = "_putchar";
	int ch
	
	for (ch =0; ch < 8; ch++)
		_putchar('/n';

	return (write(1, &c, 1));
}
